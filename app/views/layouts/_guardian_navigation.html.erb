<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav metismenu" id="side-menu">
      <li class="nav-header">
        <div class='dropdown profile-element'>
          <a data-toggle='dropdown' class='dropdown-toggle no-bg' href='#'>
            <%= image_tag current_user.avatar.url(:thumb), width: 50, height: 50, class: 'img-circle thumb-circle-border' %>

            <span class='clear'>
              <span class='block m-t-xs'>
                <strong class='font-bold'><%= current_user.name %></strong>
              </span>

              <span class='text-muted text-xs block'>
                <%= current_user.class.name.capitalize %> <b class='caret'></b>
              </span>
            </span>
          </a>

          <ul class='dropdown-menu animated fadeInRight m-t-xs'>
            <li><%= link_to 'Account', account_path %></li>
            <li><%= link_to 'Notice Board', notices_path %></li>

            <li class='divider'></li>

            <li><%= link_to 'Logout', get_logout_path(current_user), method: :delete %></li>
          </ul>
        </div>

        <div class='logo-element'>RC+</div>
      </li>

      <li class="<%= is_active_controller_action('guardian/home', 'index') %>">
        <%= link_to root_path do %>
          <i class="fa fa-th-large"></i> <span class="nav-label">Dashboard</span>
        <% end %>
      </li>
      <% if selected_student.present? %>
        <li class="<%= is_active_controller_action('guardian/attendance', 'report') %>">
          <%= link_to report_attendance_path do %>
            <i class="fa fa-calendar"></i> <span class="nav-label">Attendance Report</span>
          <% end %>
        </li>
        <li class="<%= is_active_controller_action('guardian/assignments', 'index') %>">
          <%= link_to assignments_path(section_id: selected_student.current_section(current_term.id)) do %>
            <i class="fa fa-tasks"></i> <span class="nav-label">Assignments</span>
          <% end %>
        </li>
        <li class="<%= is_active_controller('guardian/exams') %>">
          <%= link_to exams_path do %>
            <i class="fa fa-list-alt" aria-hidden="true"></i> Exam List
          <% end %>
        </li>
        <li>
          <%= link_to '#', data: { toggle: 'modal', target: '#feedback-modal' }, id: 'feedback-model-trigger' do %>
            <i class="fa fa-comments-o"></i> <span class="nav-label">Feedback</span>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

<%= render 'testimonials/form' %>
