<div class="modal inmodal" id="term-modal-<%= term.id %>" tabindex="-1" role="dialog" aria-hidden="true" style="display: none;">
  <div class="modal-dialog modal-md">
    <div class="modal-content animated flipInY">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
        <i class="fa fa-edit modal-icon m-l-md"></i>
        <h4 class="modal-title">Actions</h4>
      </div>
      <div class="modal-body">
      <div class='row text-center'>
        <%= button_to "Switch to #{term.name}", update_selected_term_administrator_term_path(term), { class: 'btn btn-success m-t-md', disable_with: 'Switching', method: :put } %>
        <%= link_to "Edit #{term.name}", edit_administrator_term_path(term), class: 'btn btn-primary m-t-md' %>
      </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-white" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>
