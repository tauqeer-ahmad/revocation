<div class='row wrapper border-bottom white-bg page-heading'>
  <div class='col-lg-9'>
    <h2>
      <%= link_to :back, class: 'btn btn-sm btn-primary btn-outline m-t-n-xs' do %>
        <i class='fa fa-arrow-left'></i>
      <% end %>

      Diary Notes
    </h2>

    <ol class='breadcrumb'>
      <li>
        <%= link_to 'Home', root_path %>
      </li>

      <li>
        <%= link_to 'Diary Notes', list_diary_notes_path(@diary_note.section_id, @diary_note.subject_id) %>
      </li>

      <li class='active'>
        <strong>New</strong>
      </li>
    </ol>
  </div>
</div>

<div class='wrapper wrapper-content animated fadeInRight'>
  <div class='row'>
    <div class='col-sm-12'>
      <div class="ibox float-e-margins">
        <div class="ibox-title">
          <h5>
            Diary Note - <%= @diary_note.section.klass.name %> - <%= @diary_note.section.name %> - <%= @diary_note.subject.name %>
          </h5>

          <div class="ibox-tools">
            <a class="collapse-link">
              <i class="fa fa-chevron-up"></i>
            </a>
          </div>
        </div>

        <div class="ibox-content">
          <%= form_for(@diary_note, html: {class: "form-vertical", id: "diary-note-form"}) do |f| %>
            <%= render partial: 'teacher/diary_notes/form_content', locals: {f: f} %>
            <%= f.hidden_field :section_id %>
            <%= f.hidden_field :subject_id %>
            <hr/>
            <div class= 'row m-t-lg'>
              <div class='col-md-12'>
                <%= submit_tag "Publish", class: "btn btn-success m-l-md pull-right" %>
                <%= link_to 'Back', list_diary_notes_path(section_id: @diary_note.section_id, subject_id: @diary_note.subject_id), class: 'btn btn-info pull-right' %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
